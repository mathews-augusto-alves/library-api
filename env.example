# ========================================
# API Library - Variáveis de Ambiente
# ========================================
# Copie este arquivo para .env e ajuste os valores

# ========================================
# DATABASE (PostgreSQL)
# ========================================
# URL completa do banco (prioridade)
DATABASE_URL=postgresql://appuser:apppassword@localhost:5432/appdb

# Configurações individuais (fallback se DATABASE_URL não estiver definida)
POSTGRES_USER=appuser
POSTGRES_PASSWORD=apppassword
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=appdb

# ========================================
# REDIS (Cache)
# ========================================
# URL completa do Redis (prioridade)
REDIS_URL=redis://localhost:6379

# Configurações individuais (fallback se REDIS_URL não estiver definida)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_ENABLED=true

# ========================================
# APPLICATION
# ========================================
# Timezone da aplicação
APP_TZ=America/Sao_Paulo

# JWT Authentication
JWT_SECRET=dev-secret-change-in-production
JWT_EXPIRES_SECONDS=3600

# Logging
LOG_LEVEL=INFO

# ========================================
# DESENVOLVIMENTO LOCAL
# ========================================
# Para rodar apenas a aplicação (sem infraestrutura):
# - Comente REDIS_ENABLED para desabilitar cache
# - A aplicação usará fallbacks automaticamente

# Exemplo para desenvolvimento sem infraestrutura:
# REDIS_ENABLED=false

# ========================================
# DOCKER
# ========================================
# Para Docker, use:
# DATABASE_URL=postgresql://appuser:apppassword@postgres:5432/appdb
# REDIS_URL=redis://redis:6379 